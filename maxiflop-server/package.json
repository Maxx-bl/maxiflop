{
  "name": "maxiflop-server",
  "version": "1.0.0",
  "private": true,
  "description": "Serveur lobby + websocket pour Maxiflop",
  "main": "server.js",
  "scripts": {
    "start": "npm install && node server.js"
  },
  "dependencies": {
    "cloudflared": "^0.7.1",
    "express": "^4.21.2",
    "socket.io": "^4.8.3"
  }
}
